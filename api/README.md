# API Domain Boundary

This directory is reserved as the canonical API ownership boundary for contracts, schemas, and composition layers.

Active API runtime implementation currently lives under `services/api`.
